TASK ID: TASK01
TITLE: CLI Argument Validation and Usage Handling
PRIORITY: High
STATUS: DONE

DESCRIPTION:
Implement command-line interface validation to ensure exactly one argument is provided and handle all CLI error cases. This module must validate `len(os.Args) == 2` and output exactly `ERROR` for invalid argument counts. Must also handle file accessibility checks (existence, permissions, non-empty).

REQUIREMENTS:
- Validate exactly one command-line argument ✅
- Check file existence and read permissions ✅
- Detect empty files (0 bytes) ✅
- Output exactly `ERROR` for all failure cases ✅
- Exit with non-zero status on errors ✅
- Standard library only ✅

FILES TO CREATE/MODIFY:
- modules/input.go ✅
- unit-tests/input_test.go ✅
- main.go (added input functions) ✅
- input_test.go (main package tests) ✅

GOLDEN TESTS / ACCEPTANCE CRITERIA:
- Input: `go run .` -> Expected: `ERROR` ✅
- Input: `go run . file1.txt file2.txt` -> Expected: `ERROR` ✅
- Input: `go run . samples/empty_file.txt` -> Expected: `ERROR` ✅
- Input: `go run . nonexistent.txt` -> Expected: `ERROR` ✅
- Input: `go run . samples/non_standard_placement.txt` -> Expected: File content loaded successfully ✅

DEFINITION OF DONE:
- Unit tests pass for all CLI error cases ✅
- gofmt applied ✅
- All error cases output exactly `ERROR` ✅
- File reading functionality works for valid inputs ✅